using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using BTManager;
using StateMachine;

namespace UI
{
    public class LoadingUI : MonoBehaviour
    {

        [SerializeField]
        private Slider loadingSlider;

        //private void Update()
        //{
        //    loadingSlider.value = Mathf.Clamp01((GameLoadingState)GameManager.Instance.currentState.)
        //}
    }
}